{
    "scripts": {
        "post": "node packScript/pack.js",
        "push": " git commit -am '文章更新' && git push && git checkout gh-pages && git merge master list.js article.js txt && git push"
      }
}